{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "devcontainer: 'echo'",
      "command": "echo",
      "args": ["${input:recipeSelection}", "${input:targetSelection}"]
    },
    {
      "label": "devcontainer: 'just build all'",
      "type": "shell",
      "command": "direnv",
      "args": ["exec", ".", "just", "build", "all"],
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent"
      }
    },
    {
      "label": "devcontainer: 'just clean'",
      "type": "shell",
      "command": "direnv",
      "args": ["exec", ".", "just", "clean"],
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent"
      }
    },
    {
      "label": "devcontainer: 'devcontainer: just build clean'",
      "type": "shell",
      "dependsOn": [
        "devcontainer: 'just clean'",
        "devcontainer: 'just build all'"
      ],
      "dependsOrder": "sequence"
    }
  ]
}
